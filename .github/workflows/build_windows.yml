name: Build Windows Standalone EXE

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build-windows:
    runs-on: windows-2022

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Install Qt 6.7.2 (MSVC 64-bit)
        uses: jurplel/install-qt-action@v3
        with:
          version: '6.7.2'
          arch: 'win64_msvc2019_64'
          cache: true

      - name: Setup MSVC Developer Command Prompt
        uses: ilammy/msvc-dev-cmd@v1

      - name: Setup Ninja
        uses: ashutoshvarma/setup-ninja@master
        with:
          version: 1.11.1

      - name: Configure CMake
        run: |
          cmake -B build -G "Ninja" -DCMAKE_BUILD_TYPE=Release

      - name: Build Application
        run: |
          cmake --build build --config Release

      - name: Deploy Windows Binary with windeployqt & MSVC Runtime
        shell: cmd
        run: |
          mkdir dist
          mkdir dist\MahadevRiceMillERP
          copy build\MahadevRiceMillERP.exe dist\MahadevRiceMillERP\
          
          REM Deploy all Qt plugins, QML imports, and compiler runtime
          windeployqt --qmldir qml --release --compiler-runtime --no-translations dist\MahadevRiceMillERP\MahadevRiceMillERP.exe
          
          REM Explicitly copy MSVC C/C++ Runtime DLLs (msvcp140.dll, vcruntime140.dll, vcruntime140_1.dll)
          powershell -Command "Get-ChildItem -Path '${env:VCToolsRedistDir}x64\Microsoft.VC143.CRT\*.dll' | Copy-Item -Destination 'dist\MahadevRiceMillERP\' -Force"
          
          REM Create distribution zip
          cd dist
          tar -a -c -f "MahadevRiceMill-Windows-x64.zip" MahadevRiceMillERP

      - name: Upload Windows Standalone Artifact
        uses: actions/upload-artifact@v4
        with:
          name: MahadevRiceMill-Windows-x64
          path: dist/MahadevRiceMill-Windows-x64.zip
